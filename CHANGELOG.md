# Changelog

本文件记录 `fund-liquidation-clause` 的版本变更。版本发布前先在对应开发分支累计记录，发布时补充日期和 Git 标签。

## [0.2.1] - Unreleased

### Fixed

- **旧契约T3漏判**：`legacy_direct` 从仅阶段3扩展到阶段2+（100人阈值老合同的”有权终止”模式在阶段2即可识别），并新增 `implicit_no_meeting` 路径——触发句中有”有权/应当/直接/宣布”类决定性终止措辞且无持有人大会字样时，所有阶段均可判为T3。修复了大成财富管理2020A（090006）和国投瑞银融华债券（121001）被误判为T1的问题。
- **T2漏判（”10个工作日”缺”内”字）**：`day_10` 模式中”内”字从阶段1的强制要求改为各阶段均可选。修复了兴业聚华A（005984）等4只合同写”10个工作日向”（缺”内”）被漏判T2的问题。
- **T2漏判（PDF断词）**：`day_10` 增加 artifact gap 容错（即使”10”和”个工作日”被页眉页脚打断也能识别），阶段1 T1结果若有”6个月”标记则 defer 到阶段2宽松复判。修复了建信普泽养老2040（012283）、南方养老2040/2045（009572/009573）等5只因PDF页眉打断”10个工作日”或”持有人大会”而漏判T2的问题。

## [0.2.0] - Unreleased

### Fixed

- 修复分类器不支持”30个工作日+1亿元”自动终止变体的问题。南方基金旗下多只偏债混合型基金（如南方宝昌A 010230）使用该条款格式，此前全部漏判为未分类。现在`day_30`模式已加入类型3触发链，所有阶段均可识别。
- 修复旧合同100人阈值+证监会批准后有权宣布终止的格式（如银河收益151002）在阶段三无法分类的问题。
- 修复”向中国证监会报告”标准语序无法识别为类型1的问题。
- 分类前必须在同一句基础条款中确认20个工作日、200人和5000万元门槛，并遍历全部有效5000万元锚点。
- 类型2/3特征限定在同一条款链内，句号、问号和感叹号会终止关联，避免孤立期限或跨句拼接误判。
- `stage=1/2/3` 分别对应阿拉伯数字严格模式、中文数字扩展模式和 OCR/旧契约容错模式；非法阶段值会明确报错。
- 阶段二先使用同一份完整基金合同按宽松规则复判，再尝试替代公告源，避免主合同严格匹配失败后直接换文档。
- Datayes 合同标题改为评分选择，排除费率调整、修改说明和公告类文档冒充完整基金合同。
- 类型3支持50日或60日触发后的清算/终止措辞，并覆盖“履行清算程序后终止”“清算并终止”等常见表达。
- 类型2支持50日或60日触发，同时继续要求10日报告和6个月大会两项期限完整共现。
- 阶段一在基础锚点仍保持严格的前提下识别中文处置期限，避免将十日报告和六个月大会提前误分为类型1。
- 类型1覆盖“说明原因并报送解决方案”和仅规定20日监管报告的旧合同表述。
- 分类器增加 PDF 页眉插入容错，并通过数字边界约束避免把5000万元误识别成50日。
- 阶段三兼容100人门槛、“基金持有人”和“基金契约”等早期合同表述。
- CSRC 查询起始日期从2019年扩展到2000年，支持分页、候选去重、标题排序及逐份失败后继续尝试。
- CSRC 下载增加 PDF 文件头校验；系列基金151002可从CSRC主索引代码151001获取标题含“系列”的合同。
- 逐阶段失败原因写入 `failureHistory`，CSRC 候选尝试写入 `candidateAttempts`，最终结果不再用通用原因覆盖。
### Performance

- 阶段一/二拆分为 API、PDF 下载、PDF 解析三个独立流式线程池，并发分别调整为 16、16、8；任一上游任务完成后即可进入下一环节。
- 同一轮运行内按 PDF URL 聚合等待任务，相同文档只下载、解析和分类一次，再将结果回填到对应基金。
- CSRC 并发由 5 提升到 8，并在阶段三入口再次过滤已有成功分类结果，只处理仍未分类基金。

### Changed

- `results_cache.json` 升级为带 `schemaVersion` 和 `pipelineVersion` 的版本化缓存；本轮 schema 提升至3，默认复用当前版本的成功结果。
- 缓存每新增 25 条成功结果及每个阶段结束时原子保存；异常中断后可继续处理剩余基金。
- 失败记录不会被缓存跳过；旧版列表缓存和不同流水线版本缓存不会自动复用。
- 新增 `--no-resume` 参数，可显式忽略缓存并从头运行。

### Tests

- 新增分类器回归测试基线，覆盖类型1/2/3、非清盘文本和5个已知缺陷。
- 分类器测试扩展至36项，新增30日变体（阿拉伯数字/中文数字）、旧合同100人+审批终止、50/60日类型2/3、清算终止、旧契约100人门槛、页眉插入、数字边界和跨句反例。
- 流水线与数据源测试扩展至15项，覆盖失败轨迹、同合同复判顺序、完整合同标题选择、CSRC分页排序/多候选/系列索引和并发配置；当前共51项测试。

## [0.1.0] - 2026-07-28

### Added

- 建立基金清盘条款三级流水线初始版本。
- 添加 Datayes 主源、替代公告源、CSRC 兜底、分类器和 Excel 输出脚本。
