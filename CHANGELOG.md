# Changelog

本文件记录 `fund-liquidation-clause` 的版本变更。版本发布前先在对应开发分支累计记录，发布时补充日期和 Git 标签。

## [0.2.0] - Unreleased

### Fixed

- 修复“向中国证监会报告”标准语序无法识别为类型1的问题。
- 类型2改为必须在60日后的同一条款链中同时匹配“10个工作日内报告”和“6个月内召集大会”，避免孤立期限造成误判。
- 类型3改为必须在50日后的同一条款链中同时匹配“自动终止”和“无需召开持有人大会”，不再把60日或无关免开大会条款误判为类型3。
- 分类前必须在同一句基础条款中同时确认20个工作日、200人和5000万元门槛。
- 60日/50日后的特征关联在句号、问号和感叹号处终止，避免跨句拼接误判。
- `stage=1/2/3` 现在分别对应阿拉伯数字严格模式、中文数字扩展模式和 OCR 分隔符容错模式；非法阶段值会明确报错。
- 遍历全部有效5000万元锚点，不再只使用首个候选上下文。

### Performance

- 阶段一/二拆分为 API、PDF 下载、PDF 解析三个独立流式线程池，并发分别调整为 16、16、8；任一上游任务完成后即可进入下一环节。
- 同一轮运行内按 PDF URL 聚合等待任务，相同文档只下载、解析和分类一次，再将结果回填到对应基金。
- CSRC 并发由 5 提升到 8，并在阶段三入口再次过滤已有成功分类结果，只处理仍未分类基金。

### Changed

- `results_cache.json` 升级为带 `schemaVersion` 和 `pipelineVersion` 的版本化缓存，默认复用当前版本的成功结果。
- 缓存每新增 25 条成功结果及每个阶段结束时原子保存；异常中断后可继续处理剩余基金。
- 失败记录不会被缓存跳过；旧版列表缓存和不同流水线版本缓存不会自动复用。
- 新增 `--no-resume` 参数，可显式忽略缓存并从头运行。

### Tests

- 新增分类器回归测试基线，覆盖类型1/2/3、非清盘文本和5个已知缺陷。
- 修复后扩展为22项分类器测试，新增中文数字、OCR 分隔符、基础门槛、类型3关联条件、跨句边界、自然日反例和非法阶段覆盖。
- 新增5项流水线测试，覆盖成功缓存复用、失败缓存重试、旧缓存隔离、CSRC 候选过滤、URL 去重和并发配置；当前共27项测试。

## [0.1.0] - 2026-07-28

### Added

- 建立基金清盘条款三级流水线初始版本。
- 添加 Datayes 主源、替代公告源、CSRC 兜底、分类器和 Excel 输出脚本。
